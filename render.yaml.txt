services:
  - type: web
    name: teskari-tezlik
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: API_TOKEN
        sync: false          # Render dashboard dan kiritiladi
      - key: TELEGRAM_MINI_APP_SHORT_NAME
        value: draw
      - key: UPSTASH_REDIS_REST_URL
        sync: false          # Render dashboard'da Upstash'dan olingan URL kiritiladi
      - key: UPSTASH_REDIS_REST_TOKEN
        sync: false          # Render dashboard'da Upstash'dan olingan TOKEN kiritiladi
      - key: RENDER_EXTERNAL_URL
        fromService:
          type: web
          name: teskari-tezlik
          property: host
          prefix: "https://"
